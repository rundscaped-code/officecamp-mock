# その場編集・タブ順・案件ページ月ナビ（2026-07-23）

背景: 経理ページ・案件ページから対象を弄るとき、プロジェクトページへ遷移させられるのが手間。編集は「今いるページの中」で完結させる。案件ページは受注日（start_date）基準の月別で見たい。

前提: restructure-project-anken.md / anken-page-and-edit-ux.md が正。DB・shared.js の変更は無し。

## 共通仕様

1. タブ順: 案件をプロジェクトの上（前）に。PC サイドバー = 案件 / プロジェクト / 経費 / タスク / 経理 / メンバー（ショートカット 1案件 2プロジェクト 3経費 4タスク 5経理）。スマホのタブバー = 経費 / 案件 / プロジェクト / タスク / 経理。既定ビュー・既定着地は変えない（PC=プロジェクト、スマホ=経費）。
2. 案件ページの月ナビ: 受注日（start_date）基準の月別表示。ヘッダに「← 2026年7月 →」の月送り（前月/翌月の矢印）と「今月」ボタン。KPI 3枚（担当案件数・粗利計・経費計）と一覧は表示月の案件のみで再計算。start_date 未設定の案件は月に関係なく一覧の下の「開始日未設定」節に常に表示する（漏れ防止）。初期表示は今月。

## PC（pc.html、担当 front-B）

3. 経理ページのその場編集: 行クリックで**ビュー遷移せず**、経理ページ内でプロジェクト詳細編集モーダルを開く（プロジェクトページへ飛ばさない）。モーダル内で編集できること: プロジェクト名・受注元・状態・リーダー・金額（空欄=null）・開始日・納品日・メモ、案件一覧（金額・状態のその場編集＋案件追加＋右クリック削除）、経費一覧（金額・メモ・申請先の編集＋削除）。既存の行編集部品（ankenRowHTML / expenseLineHTML / detailExp* / parseAmountOrNull）を流用してよい。保存のたびに経理テーブルと KPI を再描画。id 重複に注意（projects ビューの #projDetail と同じ id を同時に生かさない）。
4. 案件ページのその場編集: 行クリックで**ビュー遷移せず**、案件ページ内で案件詳細を開く（projects ビューの selectAnken へ飛ばさない）。master-detail の右ペインでもモーダルでもよい。編集できること: 案件名・状態・金額（空欄=null）・開始/終了日・担当者、この案件の経費（編集・削除・追加）とタスク（追加・状態変更）。既存の selectAnken 描画を器（コンテナ）指定で再利用する実装を推奨。
5. タイムライン等からの既存遷移（openProjPop → projects ビュー）は変えない。

## スマホ（app.html、担当 front-A。sw.js VERSION v11→v12）

6. タブ順を 経費 / 案件 / プロジェクト / タスク / 経理 に並べ替え（内部 id・hash は変えない）。
7. s-ankens に月ナビ（共通仕様2）: ヘッダ直下に「← 2026年7月 →」+「今月」。KPI・カード一覧は表示月スコープ。「開始日未設定」節を下に常設。
8. 経理タブ・案件タブからの詳細: 現状どおり詳細画面（openDetail）へ重ねる遷移でよいが、戻る（ブラウザバック/戻る導線）で元のタブ（経理なら経理、案件なら案件）へ確実に戻ること。detail の hash 履歴が直前タブを保持しているか確認し、崩れていれば直す。
9. 経理タブの行編集: 既存のセル直接編集＋長押しシートで完結しており遷移不要のため変更なし（行タップの詳細遷移は8の戻る保証のみ）。

## 分担・検証

- front-A: app.html + sw.js ／ front-B: pc.html。相互のファイル・shared.js・db/ に触らない。
- 検証: 両画面がコンソールエラーなくログイン画面まで描画。
- 検収・push はメイン。
- 仮: 月ナビの境界は start_date の暦月一致（タイムゾーンは date 型のためそのまま文字列比較で可）。月をまたぐ案件の按分はしない。
